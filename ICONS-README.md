# SVG Sprite Icons System

Кастомная система иконок на основе SVG-спрайтов для Next.js.

## 🎯 Особенности

-   ✅ Все иконки загружаются одним файлом (оптимизация)
-   ✅ Легкое изменение размера и цвета
-   ✅ TypeScript поддержка
-   ✅ Автоматическая генерация спрайта
-   ✅ Совместимость с Tailwind CSS

## 📦 Структура

```
public/
  icons/          # Исходные SVG файлы
    next.svg
  sprite.svg      # Сгенерированный спрайт
components/
  Icon/
    Icon.tsx      # Компонент иконки
    types.ts      # TypeScript типы
    index.ts      # Экспорты
scripts/
  generate-sprite.js  # Скрипт генерации
```

## 🚀 Использование

### 1. Добавление новых иконок

Поместите SVG файлы в папку `public/icons/`

### 2. Генерация спрайта

```bash
npm run generate:sprite
```

### 3. Использование компонента

```tsx
import { Icon } from '@/components/Icon';

// Базовое использование
<Icon name="next" />

// С кастомным размером
<Icon name="next" size={48} />

// С кастомным цветом
<Icon name="next" color="#0070f3" />

// С Tailwind классами
<Icon name="next" className="text-blue-500" />
```

## 🎨 Props

| Prop        | Тип      | По умолчанию     | Описание                        |
| ----------- | -------- | ---------------- | ------------------------------- |
| `name`      | `string` | -                | Имя иконки (имя файла без .svg) |
| `size`      | `number` | `24`             | Размер в пикселях               |
| `color`     | `string` | `'currentColor'` | Цвет заливки                    |
| `className` | `string` | `''`             | CSS классы                      |

## 🔄 Автоматическая генерация при сборке

Спрайт автоматически генерируется при выполнении `npm run build`.

## 💡 Примеры

```tsx
// Разные размеры
<Icon name="next" size={16} />
<Icon name="next" size={24} />
<Icon name="next" size={32} />
<Icon name="next" size={48} />

// С цветами
<Icon name="next" color="#000000" />
<Icon name="next" color="#0070f3" />

// С Tailwind
<Icon name="next" className="text-red-500 hover:text-red-700" />
<Icon name="next" className="w-8 h-8 text-green-600" />
```

## 📝 Добавление типов для новых иконок

Отредактируйте `components/Icon/types.ts`:

```typescript
export type IconName = 'next' | 'arrow' | 'user' | 'settings';
```

Это обеспечит автодополнение и проверку типов в TypeScript.
